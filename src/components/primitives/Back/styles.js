// Dependencies
import styled from 'styled-components';
// Externals
import BackIcon from 'components/primitives/BackIcon';

export const Wrapper = styled.div`
  align-items: center;
  cursor: pointer;
  display: flex;
  flex-shrink: 0;
  flex: 1;
`;

export const StyledArrow = styled(BackIcon)``;
